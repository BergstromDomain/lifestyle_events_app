<h3 class="text-center">Adding New Lifestyle Event Type</h3>

<div class="row">
  <div class="col-md-12">
    <%= form_for(@lifestyle_event_type, :html => {class: "form-horizontal", role: "form"}) do |f| %>
      <% if @lifestyle_event_type.errors.any? %>
        <div class="panel panel-danger col-md-offset-1">
          <div class= "panel-heading">
            <h3 class="panel-title">
              <%= pluralize(@lifestyle_event_type.errors.count, "error") %>
              prohibited this lifestyle event type from being saved:
            </h3>

            <div class="panel-body">
              <ul>
                <% @lifestyle_event_type.errors.full_messages.each do |msg| %>
                  <li>
                    <%= msg %>
                  </li>
                <% end %>
              </ul>
            </div>
          </div>
        </div>
      <% end %>
      <div class="form-group">
        <div class="control-label col-md-1">
          <%= f.label :title %>
        </div>
        <div class="col-md-11">
          <%= f.text_field :title, class: "form-control", placeholder: "Title of lifestyle event type", autofocus: true %>
        </div>
      </div>

      <div class="form-group">
        <div class="col-md-offset-1 col-md-11">
          <%= f.submit "Create Lifestyle Event Type", class: "btn btn-primary btn-lg pull-right" %>
        </div>
      </div>
    <% end %>
  </div>
</div>
